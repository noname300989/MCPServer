# MCPServer

This is a Model Context Protocol (MCP) server for terminal commands.

## Features
- Provides tools to interact with the terminal and execute commands securely.
- Built using the MCP Python SDK.

## Setup
1. Ensure you have Python installed.
2. Install dependencies using `uv`.
3. Run the server using `mcp run main.py`.
